# 회전

# ABC 를 맨 뒤로 보내는 작업을 M번
# quee 쓰기 pop 쓰기


# 화덕굽기
# 1~ M까지 M개의 피자를 화덕에 넣을때
# 치즈의 양별로 꺼내지는 순서가 다름
# 치즈는 한바퀴마다 N/2로 줄어듦

# 과제
from collections import deque
arr = list(map(str, input()))

print(arr)